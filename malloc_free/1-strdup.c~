#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * main - check the code
 *
 * Return: Always 0.
 */

char *_strdup(char *str)
{
	int i;
	char *s;

	if (str == 0)
	{
		return (0);
	}
	s malloc(sizeof (char) * (i + 1));
	if (s == 0)
	{
		return (0);
	}
	s[i] = '\0';
	while (i--)
	{
		s[i] = str[i]
	}
	return (s);
}
