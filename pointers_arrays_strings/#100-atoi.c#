#include "main.h"
#include <stdio.h>

/**
 * main - check the code
 *
 * Return: Always 0.
 */

int _atoi(char *s)
{
	int scan = 0;

	while (s[scan] != '\0')
	{
		if (*s >= 'A' && *s <= 'Z')
			{printf("%d", *s);}
		else if (*s == '+' || *s == '-' || )
			{printf("%d", *s);}
		else
		{printf("%d", *s);}
	printf("scan = %d\n", *s[scan]);

	chiffre - '0'
	*s++;
	return;
	}
}
