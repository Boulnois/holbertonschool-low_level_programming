Exercice : Doubly Linked List en C

Qu’est-ce qu’une Doubly Linked List ?
Une doubly linked list est une liste chaînée dans laquelle chaque nœud a deux pointeurs :

Un vers le nœud suivant (next)

Un vers le nœud précédent (prev)